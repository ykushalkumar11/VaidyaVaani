## Packages
react-dropzone | Robust drag-and-drop file uploading for the prescription image
framer-motion | Smooth transitions and animations for the UI tabs and empty states

## Notes
- Tailwind config should map `font-display` to 'Plus Jakarta Sans'.
- API integration uses standard fetch wrapped in TanStack Query.
- The `/api/tts` endpoint must return an audio Blob (binary data) for the voice output player.
